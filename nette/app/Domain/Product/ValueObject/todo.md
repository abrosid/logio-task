This folder contains the value objects for the Product domain, 
especially the ProductId and other required value objects, 
which are used to represent the product's identity and other attributes in a type-safe manner.
Value objects are immutable objects that represent a specific value or concept in the domain. 
They are used to encapsulate and validate data, ensuring that it adheres to the business rules and constraints of the domain.